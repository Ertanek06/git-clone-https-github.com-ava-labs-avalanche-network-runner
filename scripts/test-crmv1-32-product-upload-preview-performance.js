import assert from "node:assert/strict";
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const read = (file) => fs.readFileSync(path.join(root, file), "utf8");
const pkg = JSON.parse(read("package.json"));
const build = JSON.parse(read("BUILD_INFO.json"));
const upload = read("src/middleware/upload.js");
const products = read("src/routes/products.js");
const form = read("views/products/form.ejs");
const app = read("public/js/app.js");
const render = read("src/services/quote-render.service.js");
const access = read("src/services/upload-access.service.js");
const migrate = read("src/db/migrate.js");
const operational = read("src/services/operational-schema.service.js");

assert.equal(pkg.version, "3.8.57");
assert.equal(build.build, "crmv1.45");
assert.equal(build.release, "v3.8.57-crmv1.45-web-import-upsert-ui-document-fix");
assert.match(upload, /export const productUpload = multer/);
assert.match(upload, /120 \* 1024 \* 1024/);
assert.match(upload, /application\/x-pdf/);
assert.match(upload, /application\/octet-stream/);
assert.match(upload, /Buffer\.from\("%PDF-", "ascii"\)/);
assert.match(products, /productUpload\.fields\(\[/);
assert.match(products, /cleanupUploadedFiles\(req\)/);
assert.match(products, /documents:\s*\{/);
assert.match(form, /data-product-save-form/);
assert.match(form, /data-product-save-status/);
assert.match(app, /form\[data-product-save-form\]/);
assert.match(app, /crm:preview-dom-ready-v132/);
assert.match(app, /setTimeout\(revealPreview,4500\)/);
assert.match(render, /inlineAssets = !embeddedPreview/);
assert.match(render, /inlineAssets \? inlineQuotePrintImages\(row\) : row/);
assert.doesNotMatch(access, /FROM quote_items WHERE tenant_id=\? AND quote_id=\?/);
assert.match(access, /JOIN quotes q ON q\.id=i\.quote_id/);
assert.match(migrate, /idx_quote_items_product_lookup/);
assert.match(migrate, /idx_quotes_customer_lookup/);
assert.match(operational, /idx_quote_items_product_lookup/);
console.log("CRMV1.32_PRODUCT_UPLOAD_PREVIEW_PERFORMANCE=24/24 OK");
